---
phase: 18-how-it-works-portfolio-benefits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/marketing-constants.ts
  - components/marketing/how-it-works.tsx
  - components/marketing/benefits.tsx
autonomous: true
requirements: [PAGE-05, PAGE-07]

must_haves:
  truths:
    - "How It Works section shows 3 numbered step cards with icons, connecting lines on desktop, vertical timeline on mobile"
    - "Benefits section shows 5 outcome-focused differentiators with icons, heading, and description"
    - "All copy matches PRD-locked content exactly"
  artifacts:
    - path: "lib/marketing-constants.ts"
      provides: "Updated HOW_IT_WORKS and BENEFITS copy matching PRD"
      contains: "We Research Your Business"
    - path: "components/marketing/how-it-works.tsx"
      provides: "3-step process section with connecting lines and mobile timeline"
      exports: ["default"]
      min_lines: 60
    - path: "components/marketing/benefits.tsx"
      provides: "5-benefit section with icons, headings, descriptions"
      exports: ["default"]
      min_lines: 50
  key_links:
    - from: "components/marketing/how-it-works.tsx"
      to: "lib/marketing-constants.ts"
      via: "import HOW_IT_WORKS"
      pattern: "import.*HOW_IT_WORKS.*from.*marketing-constants"
    - from: "components/marketing/benefits.tsx"
      to: "lib/marketing-constants.ts"
      via: "import BENEFITS"
      pattern: "import.*BENEFITS.*from.*marketing-constants"
    - from: "components/marketing/how-it-works.tsx"
      to: "hooks/use-scroll-animation"
      via: "useScrollAnimation hook"
      pattern: "useScrollAnimation"
---

<objective>
Update marketing copy to match PRD-locked content and build the HowItWorks and Benefits section components.

Purpose: These two sections bookend the portfolio — HowItWorks explains the process, Benefits sells the outcome. Both are required for Phase 18 completion.
Output: Updated constants file, two new components in components/marketing/
</objective>

<execution_context>
@/Users/sohail/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sohail/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-how-it-works-portfolio-benefits/18-CONTEXT.md
@.planning/phases/17-hero-trust-problem/17-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/marketing-constants.ts:
```typescript
const HOW_IT_WORKS = {
  sectionTitle: "How It Works",
  sectionSubtitle: "From first conversation to live site in three straightforward steps.",
  steps: [
    { number: 1, title: "...", description: "...", icon: "MessageSquare" },
    { number: 2, title: "...", description: "...", icon: "Palette" },
    { number: 3, title: "...", description: "...", icon: "Rocket" },
  ],
} as const

const BENEFITS = {
  sectionTitle: "Why Choose Somosite",
  items: [
    { icon: "Zap", title: "...", description: "..." },
    // ... 6 items currently
  ],
} as const

export type BenefitItem = (typeof BENEFITS.items)[number]
```

From hooks/use-scroll-animation.ts:
```typescript
export function useScrollAnimation(
  options?: { threshold?: number; rootMargin?: string }
): React.RefObject<HTMLElement | null>
```

From components/marketing/trust-bar.tsx (Lucide icon map pattern):
```typescript
const ICON_MAP = { CheckCircle, Shield, BarChart, Clock } as const
// Usage: const Icon = ICON_MAP[signal.icon as keyof typeof ICON_MAP]
```

Existing section markup pattern in app/(marketing)/page.tsx:
- Dark sections: `bg-[var(--mkt-bg-alt)] text-[var(--mkt-text-dark)]`
- Light/dark sections: default (inherits dark bg from layout)
- Max width: `mx-auto max-w-[var(--mkt-max-width)] px-4 sm:px-6 lg:px-8`
- Section: `py-16 sm:py-24 scroll-mt-20`
- Heading: `text-[28px] sm:text-[40px] lg:text-[48px] leading-tight text-center`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update marketing-constants.ts with PRD-locked copy</name>
  <files>lib/marketing-constants.ts</files>
  <action>
Update HOW_IT_WORKS and BENEFITS constants to match the PRD-locked copy from 18-CONTEXT.md. Also update PORTFOLIO items to match the PRD-specified businesses.

**HOW_IT_WORKS.steps** — replace all 3 steps:
1. number: 1, icon: "Search", title: "We Research Your Business", description: "We study your services, reviews, location, and market. No questionnaires. No back-and-forth. We use real data about your actual business."
2. number: 2, icon: "Palette", title: "We Build Your Custom Website", description: "Our design system creates a professional, mobile-optimized website using your real business information. No templates. No stock content."
3. number: 3, icon: "Rocket", title: "You Review and Go Live", description: "Preview your website, request any changes, and go live on your own domain. Full support included. Ready in days, not weeks."

Keep sectionTitle and sectionSubtitle as-is (they work fine).

**BENEFITS** — replace items array with exactly 5 items (currently 6):
1. icon: "Database", title: "Built From Your Real Data", description: "We research your business using the same data your customers see. The result feels like it was written by someone who knows your business."
2. icon: "Fingerprint", title: "Looks Custom, Not Cookie-Cutter", description: "Every website is designed individually. Different colors, layouts, and content. No two sites look the same."
3. icon: "Smartphone", title: "Mobile-First, SEO-Ready", description: "Every site loads fast, looks great on phones, and is structured for search engines."
4. icon: "Zap", title: "Ready in Days, Not Weeks", description: "Traditional agencies take 4-8 weeks. We deliver a preview within days."
5. icon: "ShieldCheck", title: "You Own Everything", description: "Your website, your domain, your content. No monthly subscriptions. One price, full ownership."

**PORTFOLIO.items** — replace with 6 PRD-specified businesses:
1. name: "The Olive Table", category: "Italian Restaurant", image: "/marketing/portfolio/restaurant.jpg", demoUrl: "/preview/the-olive-table"
2. name: "Bright Smile Dental", category: "Family Dental Practice", image: "/marketing/portfolio/dental.jpg", demoUrl: "/preview/bright-smile-dental"
3. name: "Morrison & Associates", category: "Personal Injury Law", image: "/marketing/portfolio/law.jpg", demoUrl: "/preview/morrison-associates"
4. name: "Elite Auto Detailing", category: "Mobile Car Detailing", image: "/marketing/portfolio/auto.jpg", demoUrl: "/preview/elite-auto-detailing"
5. name: "Flow Yoga Studio", category: "Yoga & Wellness", image: "/marketing/portfolio/yoga.jpg", demoUrl: "/preview/flow-yoga-studio"
6. name: "The Gentleman's Cut", category: "Barbershop", image: "/marketing/portfolio/barbershop.jpg", demoUrl: "/preview/the-gentlemans-cut"

Also add a `qualityBadges` array to PORTFOLIO:
```typescript
qualityBadges: [
  { icon: "Gauge", label: "PageSpeed 95+" },
  { icon: "Smartphone", label: "Mobile Responsive" },
  { icon: "Database", label: "Built From Real Data" },
]
```

Do NOT change any other constants (NAV, HERO, TRUST_SIGNALS, PROBLEM, PRICING, FAQ, FINAL_CTA, CONTACT, FOOTER).
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>HOW_IT_WORKS has 3 PRD-locked steps, BENEFITS has 5 PRD-locked items, PORTFOLIO has 6 PRD-specified businesses + qualityBadges array. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create HowItWorks and Benefits components</name>
  <files>components/marketing/how-it-works.tsx, components/marketing/benefits.tsx</files>
  <action>
**components/marketing/how-it-works.tsx:**
Create a "use client" component following established patterns (useScrollAnimation, Lucide icon map, marketing CSS variables).

Structure:
- Section with id="how-it-works", light bg (bg-[var(--mkt-bg-alt)] text-[var(--mkt-text-dark)])
- Section heading + subtitle (standard marketing heading sizes)
- 3 step cards in a horizontal row on desktop (grid-cols-3), vertical stack on mobile
- Each card: large formatted step number (01, 02, 03) in accent color, Lucide icon, title, description
- **Connecting lines on desktop:** Use CSS pseudo-elements or a horizontal dashed/dotted line between cards. Implementation: a `relative` container with `::before` pseudo-element on cards 2 and 3 that draws a horizontal line connecting to the previous card. Use `hidden sm:block` to hide on mobile. Alternative: use border-top on a connecting element between cards.
- **Vertical timeline on mobile:** On mobile (below sm breakpoint), show a vertical line on the left side connecting the steps. Each step has a numbered circle on the line. Use `border-left` on a wrapper div with `absolute` positioned number circles.
- Apply useScrollAnimation to the section ref
- Import icons: Search, Palette, Rocket from lucide-react
- Use ICON_MAP pattern from trust-bar.tsx for dynamic icon rendering

**components/marketing/benefits.tsx:**
Create a "use client" component.

Structure:
- Section with id="benefits", light bg (bg-[var(--mkt-bg-alt)] text-[var(--mkt-text-dark)])
- Section heading (standard marketing heading sizes)
- 5 benefit items in a grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (2 items per row on tablet, 3 on desktop, 1 on mobile — last row will have 2 items centered or left-aligned)
- Each item: Lucide icon in a rounded circle with accent background, heading (text-xl), and 2-3 line description
- Card hover effect: `hover:bg-[var(--mkt-surface-hover)]` with `transition-colors` (but since this is a light section, use hover:bg-gray-100 or similar subtle effect appropriate for light bg)
- Import icons: Database, Fingerprint, Smartphone, Zap, ShieldCheck from lucide-react
- Use ICON_MAP pattern for dynamic icon rendering
- Apply useScrollAnimation to the section ref

Both components follow these patterns exactly:
- "use client" directive
- Import from @/lib/marketing-constants and @/hooks/use-scroll-animation
- Ref cast: `ref={sectionRef as React.RefObject<HTMLElement>}`
- Standard section padding: py-16 sm:py-24, scroll-mt-20
- Standard max-width container
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>how-it-works.tsx renders 3 step cards with connecting lines on desktop and vertical timeline on mobile. benefits.tsx renders 5 benefit items in a responsive grid with icons. Both use useScrollAnimation and read from marketing-constants.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no new type errors)
- Both component files exist in components/marketing/
- Components import from marketing-constants and use-scroll-animation
- marketing-constants.ts HOW_IT_WORKS steps match PRD copy exactly
- marketing-constants.ts BENEFITS has exactly 5 items matching PRD copy
- marketing-constants.ts PORTFOLIO has 6 PRD-specified businesses
</verification>

<success_criteria>
- HowItWorks component renders 3 numbered steps with icons, connecting visual between steps on desktop, vertical timeline on mobile
- Benefits component renders 5 outcome-focused differentiators in a responsive grid with icons
- All copy matches PRD-locked content from 18-CONTEXT.md
- Both components follow established patterns (useScrollAnimation, ICON_MAP, marketing tokens)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-how-it-works-portfolio-benefits/18-01-SUMMARY.md`
</output>
