---
phase: 18-how-it-works-portfolio-benefits
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - components/marketing/portfolio.tsx
  - app/(marketing)/page.tsx
autonomous: true
requirements: [PAGE-06]

must_haves:
  truths:
    - "Portfolio section displays 6 demo site screenshots in browser mockup frames"
    - "Each portfolio card shows business name, industry tag, and View Demo link"
    - "Quality badges (PageSpeed, Mobile, Real Data) appear below the portfolio grid"
    - "All 3 new sections (HowItWorks, Portfolio, Benefits) render on the landing page"
    - "Existing sections (Hero, Trust, Problem, Pricing, FAQ, etc.) still render correctly"
  artifacts:
    - path: "components/marketing/portfolio.tsx"
      provides: "6-card portfolio grid with browser mockup frames and quality badges"
      exports: ["default"]
      min_lines: 80
    - path: "app/(marketing)/page.tsx"
      provides: "Updated page importing HowItWorks, Portfolio, Benefits components"
      contains: "import HowItWorks"
  key_links:
    - from: "components/marketing/portfolio.tsx"
      to: "lib/marketing-constants.ts"
      via: "import PORTFOLIO"
      pattern: "import.*PORTFOLIO.*from.*marketing-constants"
    - from: "components/marketing/portfolio.tsx"
      to: "hooks/use-scroll-animation"
      via: "useScrollAnimation hook"
      pattern: "useScrollAnimation"
    - from: "app/(marketing)/page.tsx"
      to: "components/marketing/how-it-works.tsx"
      via: "component import"
      pattern: "import HowItWorks from.*marketing/how-it-works"
    - from: "app/(marketing)/page.tsx"
      to: "components/marketing/portfolio.tsx"
      via: "component import"
      pattern: "import Portfolio from.*marketing/portfolio"
    - from: "app/(marketing)/page.tsx"
      to: "components/marketing/benefits.tsx"
      via: "component import"
      pattern: "import Benefits from.*marketing/benefits"
---

<objective>
Build the Portfolio showcase component and wire all 3 new sections into the landing page.

Purpose: Portfolio is the visual proof of competence — 6 demo site screenshots in polished browser frames convince prospects the agency does real work. Wiring completes Phase 18.
Output: Portfolio component, updated page.tsx replacing inline placeholders with imported components
</objective>

<execution_context>
@/Users/sohail/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sohail/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-how-it-works-portfolio-benefits/18-CONTEXT.md
@.planning/phases/18-how-it-works-portfolio-benefits/18-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output + existing codebase -->

From lib/marketing-constants.ts (after Plan 01 updates):
```typescript
const PORTFOLIO = {
  sectionTitle: "Our Work",
  sectionSubtitle: "Precision-crafted websites across industries...",
  items: [
    { name: "The Olive Table", category: "Italian Restaurant", image: "...", demoUrl: "/preview/the-olive-table" },
    // ... 6 items total
  ],
  qualityBadges: [
    { icon: "Gauge", label: "PageSpeed 95+" },
    { icon: "Smartphone", label: "Mobile Responsive" },
    { icon: "Database", label: "Built From Real Data" },
  ],
} as const
```

From components/marketing/hero.tsx (BrowserMockup pattern to reuse):
```typescript
function BrowserMockup() {
  // Desktop frame with traffic light dots, URL bar, gradient body
  // Structure: rounded-xl border, title bar with 3 dots + url, aspect-video gradient body
  // Mobile frame offset bottom-right at w-[35%]
}
```

From hooks/use-scroll-animation.ts:
```typescript
export function useScrollAnimation(
  options?: { threshold?: number; rootMargin?: string }
): React.RefObject<HTMLElement | null>
```

Current page.tsx structure (sections to replace):
- Lines 24-48: How It Works inline section -> replace with <HowItWorks />
- Lines 50-76: Portfolio inline section -> replace with <Portfolio />
- Lines 78-96: Benefits inline section -> replace with <Benefits />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Portfolio component with browser mockup cards and quality badges</name>
  <files>components/marketing/portfolio.tsx</files>
  <action>
Create a "use client" component following established patterns.

**Section structure:**
- Section id="portfolio", dark bg (default — no bg-alt class since dark sections inherit from layout)
- Section heading + subtitle (standard marketing sizes)
- 6 portfolio cards in a responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8`
- Quality badges row below the grid

**Each portfolio card:**
- A mini browser mockup frame (simplified version of hero's BrowserMockup — do NOT import from hero, create inline):
  - Rounded container with `border border-[var(--mkt-border)]` and `bg-[#111]` and `overflow-hidden`
  - Title bar: `h-7 flex items-center gap-1.5 px-3 bg-[#1A1A1A] border-b border-[var(--mkt-border)]` with 3 traffic light dots (w-2 h-2 each)
  - Screenshot area: `aspect-video` with a unique gradient background per card to simulate different websites. Use distinct color combinations per industry:
    - Restaurant: from-[#1a0f0a] to-[#2a1a10] (warm)
    - Dental: from-[#0a1a1a] to-[#102a2a] (teal)
    - Law: from-[#0f0a1a] to-[#1a102a] (deep purple)
    - Auto: from-[#1a1a0a] to-[#2a2a10] (gold)
    - Yoga: from-[#0a1a0f] to-[#102a1a] (green)
    - Barbershop: from-[#1a0a0a] to-[#2a1010] (red)
  - Inside the screenshot area, add subtle placeholder lines (like hero mockup) suggesting a website layout: a nav bar line, a headline block, a CTA button block, some content blocks
- Below the mockup frame: business name (text-lg font-medium), category tag (text-sm text-[var(--mkt-text-tertiary)]), and "View Demo" link
- The "View Demo" link: `text-sm text-[var(--mkt-accent)] hover:underline` pointing to item.demoUrl
- Card hover: `group` class on wrapper, `group-hover:border-[var(--mkt-accent)]/30 transition-colors` on the frame, `group-hover:translate-y-[-2px] transition-transform` on the whole card

**Quality badges row:**
- Centered flex row below the grid with gap-6, mt-12
- Each badge: pill shape with `inline-flex items-center gap-2 px-4 py-2 rounded-full border border-[var(--mkt-border)] text-sm text-[var(--mkt-text-secondary)]`
- Lucide icons: Gauge, Smartphone, Database (import from lucide-react)
- Use ICON_MAP pattern for dynamic icon rendering from qualityBadges array

**Scroll animation:** Apply useScrollAnimation to the section ref.

**Do NOT import BrowserMockup from hero.tsx** — the portfolio version is simpler (no mobile frame, no float animation, smaller). Keep it self-contained.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>Portfolio component renders 6 cards with browser mockup frames (each with unique industry gradient), business name, category, View Demo link. Quality badges appear below the grid. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HowItWorks, Portfolio, and Benefits into page.tsx</name>
  <files>app/(marketing)/page.tsx</files>
  <action>
Replace the 3 inline sections in page.tsx with imported components from Plan 01 and Task 1.

**Imports to add:**
```typescript
import HowItWorks from "@/components/marketing/how-it-works"
import Portfolio from "@/components/marketing/portfolio"
import Benefits from "@/components/marketing/benefits"
```

**Replacements:**
1. Remove the entire inline How It Works section (the `<section id="how-it-works"...>` block with its children) and replace with `<HowItWorks />`
2. Remove the entire inline Portfolio section (the `<section id="portfolio"...>` block with its children) and replace with `<Portfolio />`
3. Remove the entire inline Benefits section (the `<section id="benefits"...>` block with its children) and replace with `<Benefits />`

**Also remove unused imports** from the top of page.tsx:
- Remove `HOW_IT_WORKS`, `PORTFOLIO`, `BENEFITS` from the import destructuring since those constants are now consumed inside the components, not in page.tsx

**Keep all other sections as-is** (Pricing, FAQ, Final CTA, Contact, Footer remain inline for Phase 19-20).

The section order must remain: Navbar, Hero, TrustBar, Problem, HowItWorks, Portfolio, Benefits, Pricing, FAQ, FinalCTA, Contact, Footer.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>page.tsx imports HowItWorks, Portfolio, Benefits components. Inline sections replaced with component tags. Unused constant imports removed. All other sections unchanged. TypeScript compiles. Section order preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no new type errors)
- Portfolio component exists at components/marketing/portfolio.tsx
- page.tsx imports and renders HowItWorks, Portfolio, Benefits components
- page.tsx no longer has inline How It Works, Portfolio, or Benefits sections
- Pricing, FAQ, Final CTA, Contact, Footer sections remain inline in page.tsx
- Section order is correct: Navbar > Hero > TrustBar > Problem > HowItWorks > Portfolio > Benefits > Pricing > ...
</verification>

<success_criteria>
- All 3 new sections render on the landing page in correct order
- Portfolio shows 6 industry-specific cards with browser mockup frames and unique gradients
- Quality badges (PageSpeed 95+, Mobile Responsive, Built From Real Data) appear below portfolio grid
- View Demo links point to /preview/[slug] for each portfolio item
- Existing sections (Hero, Trust, Problem, Pricing, FAQ, etc.) still render correctly
- No unused imports or dead code in page.tsx
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-how-it-works-portfolio-benefits/18-02-SUMMARY.md`
</output>
