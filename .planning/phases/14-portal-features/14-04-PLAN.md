---
phase: 14-portal-features
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - app/(portal)/portal/(dashboard)/support/page.tsx
  - app/(portal)/portal/(dashboard)/support/support-client.tsx
  - app/api/portal/payments/agent/route.ts
  - app/api/webhooks/razorpay/route.ts
  - app/(portal)/portal/(dashboard)/layout.tsx
  - components/portal/need-help-button.tsx
autonomous: true
requirements: [PORTAL-07, DOMAIN-06]

must_haves:
  truths:
    - "Client can pay $49 for agent support via Razorpay from the support page"
    - "After $49 payment, an agent_call or domain_setup request is auto-created in client_requests"
    - "Support page shows agent payment CTA, WhatsApp + email contacts, and FAQ text"
    - "Floating 'Need help?' button is visible on all portal pages (dashboard, domain, customize, support)"
    - "The Razorpay webhook correctly differentiates $49 agent payments from $499/$1299 plan payments by checking payment.notes.type"
    - "$49 agent domain setup appears on Connect Existing and Buy New paths (not free subdomain) via links to /portal/support"
  artifacts:
    - path: "app/(portal)/portal/(dashboard)/support/page.tsx"
      provides: "Server component for support page"
      min_lines: 15
    - path: "app/(portal)/portal/(dashboard)/support/support-client.tsx"
      provides: "Client component with agent payment and contact info"
      min_lines: 80
    - path: "app/api/portal/payments/agent/route.ts"
      provides: "$49 Razorpay order creation endpoint"
      exports: ["POST"]
    - path: "components/portal/need-help-button.tsx"
      provides: "Floating help button component"
      min_lines: 15
  key_links:
    - from: "support-client.tsx"
      to: "/api/portal/payments/agent"
      via: "fetch POST to create Razorpay order"
      pattern: "fetch.*api/portal/payments/agent"
    - from: "support-client.tsx"
      to: "Razorpay checkout modal"
      via: "window.Razorpay with order ID"
      pattern: "Razorpay.*open"
    - from: "app/api/webhooks/razorpay/route.ts"
      to: "client_requests table"
      via: "payment.notes.type check for agent payments"
      pattern: "notes\\.type.*agent_support|domain_setup"
    - from: "app/(portal)/portal/(dashboard)/layout.tsx"
      to: "components/portal/need-help-button.tsx"
      via: "NeedHelpButton rendered in layout"
      pattern: "NeedHelpButton"
---

<objective>
Build the /portal/support page with $49 agent support payment, update the Razorpay webhook to differentiate agent payments, and add a persistent floating "Need help?" button across all portal pages.

Purpose: Agent support is the monetization path for clients who need hands-on help with domain setup or other tasks. The floating button makes this discoverable from anywhere. The webhook update ensures $49 payments create client_requests instead of touching claim status.

Output: Working support page with Razorpay $49 payment, webhook handler branching on payment.notes.type, and floating help button in portal layout.
</objective>

<execution_context>
@/Users/sohail/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sohail/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-portal-features/14-RESEARCH.md
@.planning/phases/14-portal-features/14-01-SUMMARY.md
@.planning/phases/14-portal-features/14-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/razorpay.ts:
```typescript
export const razorpay = new Razorpay({ key_id: keyId, key_secret: keySecret })
export const isRazorpayTestMode: boolean
export function getRazorpayPublicKey(): string
```

From app/api/webhooks/razorpay/route.ts (existing webhook structure):
```typescript
// Event routing in POST handler:
if (eventType === 'payment.captured') {
    await handlePaymentCaptured(payment, eventId)
} else if (eventType === 'payment.failed') {
    await handlePaymentFailed(payment, eventId)
}

// handlePaymentCaptured finds claim by razorpay_order_id, updates status to 'paid'
// MUST be extended: check payment.notes.type FIRST
// If type === 'agent_support' or 'domain_setup': create client_request instead
// If no type (or type is plan payment): proceed with existing claim update logic
```

From types/database.ts (client_requests):
```typescript
type: 'logo_upload' | 'text_change' | 'domain_setup' | 'agent_call' | 'booking_setup'
content: Json
```

From app/(portal)/portal/(dashboard)/layout.tsx (layout structure):
```typescript
// Layout renders: PortalHeader, PortalNav, <main>{children}</main>
// Floating button should be added inside the layout div, after <main>
```

From 14-RESEARCH.md (Razorpay order pattern):
```typescript
const order = await razorpay.orders.create({
    amount: 4900, // $49.00 in cents
    currency: 'USD',
    notes: {
        type: 'agent_support', // or 'domain_setup'
        claim_id, project_id, auth_user_id
    }
})
```

From 14-RESEARCH.md (pitfall #3):
```
$49 agent payment webhook hits same handler as $499/$1299.
Differentiate by payment.notes.type. If 'agent_support' or 'domain_setup':
create client_request, do NOT update claim status.
Guard with early return before claim-update logic.
```

From 14-CONTEXT.md (user decisions):
- $49 agent support: floating "Need help?" button persistent across all portal sections
- After $49 payment: confirmation + contact info + auto-created client_request of type 'agent_call'
- $49 agent domain setup appears on "Connect existing" and "Buy new" paths (not on free subdomain)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create $49 payment API, update webhook, and add floating button</name>
  <files>
    app/api/portal/payments/agent/route.ts
    app/api/webhooks/razorpay/route.ts
    components/portal/need-help-button.tsx
    app/(portal)/portal/(dashboard)/layout.tsx
  </files>
  <action>
**1. Create `app/api/portal/payments/agent/route.ts`** -- POST handler:
- Auth check via createClient + getUser, return 401 if not authenticated
- Accept JSON body: `{ type: 'agent_support' | 'domain_setup' }` validated with Zod
- Get user's claim via admin client (scoped by auth_user_id)
- Create Razorpay order: `razorpay.orders.create({ amount: 4900, currency: 'USD', notes: { type, claim_id: claim.id, project_id: claim.project_id, auth_user_id: user.id } })`
- Return `{ orderId: order.id, amount: order.amount, keyId: getRazorpayPublicKey() }`
- Set `maxDuration = 15`, `dynamic = 'force-dynamic'`
- Log with `[Portal/AgentPayment]` prefix

**2. Update `app/api/webhooks/razorpay/route.ts`** -- differentiate agent payments:
- In the `handlePaymentCaptured` function, ADD a check at the very top (before the existing claim lookup):
  ```
  const paymentType = payment.notes?.type
  if (paymentType === 'agent_support' || paymentType === 'domain_setup') {
      await handleAgentPayment(payment, eventId)
      return  // Early return -- do NOT proceed to claim update
  }
  ```
- Create new function `handleAgentPayment(payment, eventId)`:
  - Extract claim_id, project_id, auth_user_id from payment.notes
  - Validate all three exist (throw if missing so Razorpay retries)
  - Determine request type: if paymentType === 'domain_setup' use 'domain_setup', otherwise use 'agent_call'
  - Insert into client_requests: `{ claim_id, project_id, auth_user_id, type: requestType, status: 'pending', content: { description: 'Agent support requested via $49 payment', payment_id: payment.id, payment_amount: payment.amount, payment_type: paymentType } }`
  - Log: `[Webhook] Agent payment processed: ${payment.id}, type: ${paymentType}`
- The existing `handlePaymentCaptured` flow (claim status update) remains unchanged for regular plan payments (no notes.type or unrecognized type)

**3. Create `components/portal/need-help-button.tsx`** -- floating help button:
- 'use client' component
- Fixed position: bottom-right, offset from bottom nav on mobile (bottom-24 md:bottom-8 right-4)
- Appearance: round button with Headphones icon (or MessageCircle icon from lucide-react), Tailwind classes: `bg-[#0F172A] text-white rounded-full p-3 shadow-lg hover:bg-[#1e293b] transition-all hover:scale-105`
- Tooltip on hover (desktop): "Need help? Talk to our agents"
- On click: navigate to /portal/support (use `router.push('/portal/support')`)
- Use `usePathname()` to HIDE the button when already on /portal/support (don't show it there since the support page IS the help page)
- z-index: 40 (below the mobile nav which is z-50)

**4. Update `app/(portal)/portal/(dashboard)/layout.tsx`**:
- Import `NeedHelpButton` from `@/components/portal/need-help-button`
- Add `<NeedHelpButton />` inside the layout div, after `<main>` but still inside the outer div
- No other changes to the layout
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - POST /api/portal/payments/agent creates $49 Razorpay order with type in notes
    - Webhook handler checks payment.notes.type before claim update logic
    - Agent payments create client_request (not claim status update)
    - Regular plan payments still work unchanged
    - Floating help button visible on all portal pages except /portal/support
    - Button navigates to /portal/support on click
  </done>
</task>

<task type="auto">
  <name>Task 2: Create support page with payment flow and contact info</name>
  <files>
    app/(portal)/portal/(dashboard)/support/page.tsx
    app/(portal)/portal/(dashboard)/support/support-client.tsx
  </files>
  <action>
**1. Create `app/(portal)/portal/(dashboard)/support/page.tsx`** -- server component:
- Follow dashboard page pattern: auth check -> admin client -> fetch claim with plan
- Pass claim data and Razorpay public key (from `getRazorpayPublicKey()`) and test mode flag to SupportClient
- Set metadata: `title: 'Support - Flogen Portal'`

**2. Create `app/(portal)/portal/(dashboard)/support/support-client.tsx`** -- client component:
- Import Razorpay checkout script: `<Script src="https://checkout.razorpay.com/v1/checkout.js" strategy="lazyOnload" />` (using next/script)

**Section A: "Agent Support" card**
- Heading: "Get Expert Help" with subtext "Our agents can assist with domain setup, logo processing, and any customization needs"
- Price: "$49" displayed prominently with "one-time" label
- What's included list with Check icons:
  - "Domain configuration and DNS setup"
  - "Logo background removal and optimization"
  - "Website customization assistance"
  - "Priority email and WhatsApp support"
- Two CTA buttons in a row:
  - "Get Domain Help" -> triggers payment with type 'domain_setup'
  - "Get General Help" -> triggers payment with type 'agent_support'

**Payment flow** (similar to claim page Razorpay pattern):
- On CTA click: POST `/api/portal/payments/agent` with `{ type }` to create order
- Open Razorpay checkout modal with order ID, amount, key, prefill (email from claim)
- On success callback: show sonner toast "Payment successful! Our agents will reach out shortly.", show success state with contact info
- On failure: show error toast
- If test mode (prop from server): show "Test Mode" badge near the price

**Section B: "Contact Us" card**
- Heading: "Reach Out Directly"
- WhatsApp link: `https://wa.me/{WHATSAPP_NUMBER}` with MessageCircle icon and number displayed (use env var `NEXT_PUBLIC_SUPPORT_WHATSAPP` or hardcode a placeholder)
- Email link: `mailto:{SUPPORT_EMAIL}` with Mail icon and email displayed (use env var `NEXT_PUBLIC_SUPPORT_EMAIL` or hardcode a placeholder)
- Working hours note: "We typically respond within 2-4 hours during business hours"

**Section C: "FAQ" / Help Text**
- Heading: "Common Questions"
- Expandable FAQ items (reuse the expand/collapse pattern from request-card):
  - "How long does domain setup take?" -> "Once we receive your DNS records, verification typically takes 24-48 hours. Our agents can guide you through the entire process."
  - "Can I change my website after launch?" -> "Yes! Submit change requests from the Customize page and our team will update your site."
  - "What if I need more than one change?" -> "You can submit unlimited change requests. We process them in order and update your site."
  - "How do I get my own domain?" -> "Visit the Domain page to search for available domains, or connect one you already own."
  - "What's included in agent support?" -> "Our agents handle domain configuration, logo processing, content updates, and any technical issues you encounter."

**Post-payment success state:**
- After successful $49 payment, replace the payment CTA section with:
  - Green checkmark + "Payment confirmed!"
  - "Our agents will reach out within 2-4 hours via email or WhatsApp"
  - Contact info displayed inline
  - "You can also reach us directly at..." with WhatsApp and email links

Use the warm cream portal aesthetic (bg-[#f5f0ea] inherited from layout, white cards with rounded-xl border shadow-sm).
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - /portal/support renders with agent payment card, contact info, and FAQ
    - $49 payment opens Razorpay checkout modal
    - Successful payment shows confirmation with contact info
    - Test mode badge visible when RAZORPAY_MODE=test
    - WhatsApp and email links displayed for direct contact
    - FAQ section with 5 expandable items
    - Page matches portal design aesthetic
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. Dev server starts without crash
3. Floating "Need help?" button visible on /portal, /portal/domain, /portal/customize
4. Floating button NOT visible on /portal/support
5. /portal/support renders with payment CTA and contact info
6. Webhook handler correctly branches on payment.notes.type (verify by reading code -- no live Razorpay test needed)
</verification>

<success_criteria>
- $49 agent payment creates Razorpay order and opens checkout modal
- Webhook correctly differentiates agent payments from plan payments
- Agent payment creates client_request of type 'agent_call' or 'domain_setup'
- Regular $499/$1299 payments still update claim status as before
- Support page has payment CTA, WhatsApp + email contacts, and FAQ
- Floating "Need help?" button appears on all portal pages except support page itself
- Test mode badge shown when in test mode
</success_criteria>

<output>
After completion, create `.planning/phases/14-portal-features/14-04-SUMMARY.md`
</output>
