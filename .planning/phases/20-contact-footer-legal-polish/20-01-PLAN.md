---
phase: 20-contact-footer-legal-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/marketing-constants.ts
  - components/marketing/contact-form.tsx
  - components/marketing/footer.tsx
  - app/api/contact/route.ts
  - app/(marketing)/page.tsx
autonomous: true
requirements: [PAGE-11, PAGE-12, INFRA-01]
user_setup:
  - service: gmail-smtp
    why: "Contact form sends email via existing SMTP setup"
    env_vars:
      - name: CONTACT_EMAIL
        source: "Set to sohailminimalist@gmail.com (or desired recipient)"
    dashboard_config: []

must_haves:
  truths:
    - "Visitor can fill out contact form (name, email, optional business name, message) and submit"
    - "Submitting valid form sends email to CONTACT_EMAIL via existing nodemailer/Gmail SMTP"
    - "Form shows success toast after send, error message on failure, preserves input on error"
    - "Footer displays 4 columns (Company, Product, Legal, Trust) with working links"
    - "Footer shows payment method badges and copyright line"
  artifacts:
    - path: "components/marketing/contact-form.tsx"
      provides: "Client component with form state, validation, submit handler"
      min_lines: 60
    - path: "components/marketing/footer.tsx"
      provides: "Footer with 4-column grid, payment badges, copyright"
      min_lines: 40
    - path: "app/api/contact/route.ts"
      provides: "POST handler that validates input and sends email via nodemailer"
      exports: ["POST"]
    - path: "app/(marketing)/page.tsx"
      provides: "Wires ContactForm and Footer components replacing inline placeholders"
  key_links:
    - from: "components/marketing/contact-form.tsx"
      to: "/api/contact"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/contact"
    - from: "app/api/contact/route.ts"
      to: "nodemailer"
      via: "createTransport with Gmail SMTP"
      pattern: "nodemailer\\.createTransport"
    - from: "components/marketing/footer.tsx"
      to: "lib/marketing-constants.ts"
      via: "FOOTER constant import"
      pattern: "import.*FOOTER.*from.*marketing-constants"
---

<objective>
Build the contact form component with POST /api/contact email handler, and the footer component — then wire both into page.tsx replacing the inline placeholders.

Purpose: Complete the two remaining interactive sections of the landing page. Contact form converts visitors into leads. Footer provides navigation and legal links.
Output: Working contact form that sends emails, polished footer with 4 columns.
</objective>

<execution_context>
@/Users/sohail/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sohail/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-contact-footer-legal-polish/20-CONTEXT.md
@.planning/phases/19-pricing-faq-final-cta/19-01-SUMMARY.md

<interfaces>
<!-- Existing patterns and contracts the executor needs -->

From lib/marketing-constants.ts:
```typescript
const CONTACT = {
  sectionTitle: "Get In Touch",
  sectionSubtitle: "Tell us about your business and we will get back to you within 24 hours with a free proposal.",
  fields: ["name", "email", "phone", "business_name", "website_url", "message"],
} as const

const FOOTER = {
  columns: [
    { title: "Company", links: [{ label: "About", href: "#" }, ...] },
    { title: "Product", links: [...] },
    { title: "Legal", links: [{ label: "Privacy Policy", href: "/privacy" }, ...] },
    { title: "Trust", links: [{ label: "Secure Payments", href: "#" }, ...] },
  ],
  copyright: `© ${new Date().getFullYear()} Somosite. All rights reserved.`,
  paymentMethods: ["Visa", "Mastercard", "UPI"],
} as const
```

From hooks/use-scroll-animation.ts:
```typescript
export function useScrollAnimation(options?: { threshold?: number; rootMargin?: string }): React.RefObject<HTMLElement | null>
```

From app/api/admin/send-preview/route.ts (existing SMTP pattern):
```typescript
const transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: { user: process.env.SMTP_USER, pass: process.env.SMTP_PASS },
})
```

From app/(marketing)/page.tsx (current contact/footer inline code to replace):
- Lines 29-44: Inline contact section placeholder
- Lines 47-74: Inline footer section
- Imports: `import { CONTACT, FOOTER } from "@/lib/marketing-constants"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update marketing constants, create ContactForm and Footer components, create /api/contact route</name>
  <files>
    lib/marketing-constants.ts
    components/marketing/contact-form.tsx
    components/marketing/footer.tsx
    app/api/contact/route.ts
  </files>
  <action>
**1. Update lib/marketing-constants.ts CONTACT constant:**

Update the CONTACT object to match PRD-locked fields. The current CONTACT.fields array has phone, business_name, website_url which differ from the PRD spec. Update to:
- Keep sectionTitle: "Get In Touch"
- Keep sectionSubtitle as-is
- Add `submitButton: "Send Message"`
- Add `successMessage: "Thanks! We'll get back to you within 24 hours."`
- Add `microcopy: "No spam, ever. We typically respond within a few hours."`
- Remove the generic fields array (the component will define its own form structure)

Also update FOOTER to match PRD §3.12:
- Column 1 (Company): Change links to include description text. Add `description: "Custom websites built from real business data."`, `email: "hello@somosite.com"`, `location: "Bangalore, India"`. Keep links: Portfolio (#portfolio), Contact (#contact). Remove "About" link.
- Column 2 (Product): Add "Client Portal" link to /portal. Keep existing How It Works, Pricing, FAQ links. Add "Our Work" (#portfolio).
- Columns 3 (Legal) and 4 (Trust) stay as-is.
- Add `guarantee: "30-Day Satisfaction Guarantee"` and `ssl: "SSL Protected"` to FOOTER for the Trust column badges.

**2. Create components/marketing/contact-form.tsx:**

"use client" component. Pattern: match existing components (useScrollAnimation ref, CONTACT import, --mkt-* tokens).

Structure:
- Section wrapper: `id="contact"`, dark background (`bg-[var(--mkt-bg)]`), NOT the light bg-alt section. The current inline placeholder uses bg-alt but the PRD says dark background.
- Heading: CONTACT.sectionTitle (h2 with DM Serif via inheritance), subtitle below.
- Form with 4 fields per PRD: Name (required), Email (required), Business Name (optional), Message (required textarea).
- Do NOT include phone or website_url fields — PRD specifies only name, email, business name (optional), message.
- Submit button: "Send Message" with accent bg (#AF92FF), disabled + spinner during send.
- Microcopy below form: CONTACT.microcopy in muted text.

State management:
- `useState` for form data: `{ name: '', email: '', businessName: '', message: '' }`
- `useState` for status: `'idle' | 'sending' | 'success' | 'error'`
- `useState` for error message string
- On submit: `fetch('/api/contact', { method: 'POST', body: JSON.stringify(formData) })`
- On success: show success message (CONTACT.successMessage), clear form
- On error: show error message near form, do NOT clear form (preserve user input per PRD)
- Basic client-side validation: name and email required, email format check via regex

Styling:
- Input fields: dark bg (var(--mkt-surface)), white text, rounded-[var(--mkt-radius)], border var(--mkt-border), focus:border-[var(--mkt-accent)] focus:outline-none
- Use `font-[family-name:var(--font-inter-marketing)]` for form inputs (sans-serif, not DM Serif)
- Responsive: single column, max-w-2xl centered
- Use animate-on-scroll via useScrollAnimation

**3. Create components/marketing/footer.tsx:**

Server component (no "use client" needed — no state/effects). Import FOOTER from marketing-constants.

Structure per PRD §3.12:
- Dark background, border-t border-[var(--mkt-border)] separator
- 4-column grid (grid-cols-2 sm:grid-cols-4 gap-8) inside max-w container
- Column 1 special: Show "Somosite" as logo text (text-xl font-bold), description text, email, location — NOT as link list
- Columns 2-4: Title + link list (same pattern as current inline footer)
- Payment badges row: "Visa · Mastercard · UPI" as muted text (matching Phase 19 pattern)
- Trust badges: "30-Day Satisfaction Guarantee" and "SSL Protected" with check icons
- Bottom bar: copyright, separated by border-t

Anchor links (href="#section") should work as-is since layout has scroll-behavior: smooth.
Legal links (/privacy, /terms, /refund) are regular Next.js links.

**4. Create app/api/contact/route.ts:**

Reuse the exact nodemailer pattern from app/api/admin/send-preview/route.ts:
```
const transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: { user: process.env.SMTP_USER, pass: process.env.SMTP_PASS },
})
```

POST handler:
- Parse JSON body: { name, email, businessName?, message }
- Validate: name, email, message required. Email format check. Return 400 with { error: string } on failure.
- Send email to `process.env.CONTACT_EMAIL || 'sohailminimalist@gmail.com'` (fallback hardcoded per PRD)
- Email from: `"Somosite Contact" <${process.env.SMTP_USER}>`
- Email subject: `New inquiry from ${name} — Somosite`
- Email HTML body: clean professional format showing name, email, business name (if provided), message. Use inline styles (same pattern as send-preview route).
- On success: return 200 { success: true }
- On error: log error, return 500 { error: "Failed to send message. Please try again." }
- Do NOT import Supabase or any database — this is a simple email relay.
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - ContactForm component renders 4-field form with validation, submit handler, success/error states
    - Footer component renders 4-column grid with links, payment badges, copyright
    - POST /api/contact validates input and sends email via nodemailer Gmail SMTP
    - marketing-constants.ts updated with PRD-locked CONTACT and FOOTER data
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ContactForm and Footer into page.tsx</name>
  <files>
    app/(marketing)/page.tsx
  </files>
  <action>
Replace the inline contact section (lines 29-44) and footer section (lines 47-74) in page.tsx with component imports.

1. Add imports at top:
```
import ContactForm from "@/components/marketing/contact-form"
import Footer from "@/components/marketing/footer"
```

2. Remove the `import { CONTACT, FOOTER } from "@/lib/marketing-constants"` since the components now own their own constant imports.

3. Replace the inline `{/* Contact */}` section (the entire `<section id="contact">...</section>` block) with `<ContactForm />`

4. Replace the inline `<footer id="footer">...</footer>` block with `<Footer />`

The page should now have clean component imports for all 12 sections — no inline section markup remaining.
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "---" && grep -c "import.*from.*components/marketing" app/\(marketing\)/page.tsx</automated>
  </verify>
  <done>
    - page.tsx imports ContactForm and Footer components
    - No inline section markup remains in page.tsx (all sections are component imports)
    - No unused imports of CONTACT or FOOTER constants in page.tsx
    - TypeScript compiles without errors
    - Import count should be 12 (all marketing components)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors
2. `npm run build` succeeds (or `next build` for the relevant pages)
3. Visit http://localhost:3000 — contact form renders on dark background with 4 fields
4. Submit form with valid data — email sent (check SMTP logs or inbox)
5. Submit form with missing required fields — validation prevents submission
6. Footer renders 4 columns with working anchor links and legal page links
7. No inline section markup in page.tsx — all sections are component imports
</verification>

<success_criteria>
- Contact form collects name, email, business name (optional), message and POSTs to /api/contact
- /api/contact sends formatted email via existing nodemailer/Gmail SMTP setup
- Form shows success toast after send, error on failure, preserves input on error
- Footer shows 4 columns with anchor links, legal links, payment badges, copyright
- page.tsx is clean with 12 component imports and no inline markup
</success_criteria>

<output>
After completion, create `.planning/phases/20-contact-footer-legal-polish/20-01-SUMMARY.md`
</output>
