---
phase: 17-hero-trust-problem
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - components/marketing/hero.tsx
  - components/marketing/trust-bar.tsx
  - components/marketing/problem.tsx
  - app/(marketing)/page.tsx
  - app/(marketing)/layout.tsx
autonomous: true
requirements: [PAGE-02, PAGE-03, PAGE-04]

must_haves:
  truths:
    - "Hero displays headline, subheadline, two CTA buttons, and a browser mockup with float animation"
    - "Hero mockup shows desktop frame + floating mobile frame offset to the right"
    - "Trust bar shows 4 items with Lucide icons in a horizontal row (2x2 on mobile)"
    - "Problem section displays centered empathetic copy with generous whitespace"
    - "All sections animate in on scroll using the useScrollAnimation hook"
    - "All text reads from marketing-constants.ts — no hardcoded copy in components"
    - "Navbar appears above hero and sticky-scrolls with the page"
  artifacts:
    - path: "components/marketing/hero.tsx"
      provides: "Hero section with headline, CTAs, browser mockup visual"
      min_lines: 60
    - path: "components/marketing/trust-bar.tsx"
      provides: "4-item trust signal strip with Lucide icons"
      min_lines: 20
    - path: "components/marketing/problem.tsx"
      provides: "Problem statement section with empathetic copy"
      min_lines: 15
    - path: "app/(marketing)/page.tsx"
      provides: "Updated page importing Navbar, Hero, TrustBar, Problem components"
    - path: "app/(marketing)/layout.tsx"
      provides: "Updated layout with SCROLL_ANIMATION_STYLES injected"
  key_links:
    - from: "components/marketing/hero.tsx"
      to: "hooks/use-scroll-animation.ts"
      via: "import { useScrollAnimation }"
      pattern: "useScrollAnimation"
    - from: "components/marketing/hero.tsx"
      to: "lib/marketing-constants.ts"
      via: "import { HERO }"
      pattern: "HERO\\."
    - from: "components/marketing/trust-bar.tsx"
      to: "lib/marketing-constants.ts"
      via: "import { TRUST_SIGNALS }"
      pattern: "TRUST_SIGNALS"
    - from: "app/(marketing)/page.tsx"
      to: "components/marketing/*"
      via: "import Navbar, Hero, TrustBar, Problem"
      pattern: "components/marketing/"
---

<objective>
Build the three visible above-fold sections (Hero, Trust Bar, Problem) and wire all Phase 17 components into the landing page.

Purpose: These are the first things a visitor sees. The hero must create an immediate impression of professionalism with the browser mockup visual. The trust bar builds credibility. The problem section creates emotional resonance. Together they drive the visitor to scroll further.

Output: `components/marketing/hero.tsx`, `components/marketing/trust-bar.tsx`, `components/marketing/problem.tsx`, updated `app/(marketing)/page.tsx` and `layout.tsx`
</objective>

<execution_context>
@/Users/sohail/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sohail/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-hero-trust-problem/17-CONTEXT.md
@.planning/phases/17-hero-trust-problem/17-01-SUMMARY.md

@lib/marketing-constants.ts
@app/(marketing)/layout.tsx
@app/(marketing)/page.tsx
@hooks/use-scroll-animation.ts
@components/marketing/navbar.tsx

<interfaces>
<!-- From Plan 01 outputs (must exist before this plan runs) -->

From hooks/use-scroll-animation.ts:
```typescript
export function useScrollAnimation(options?: { threshold?: number; rootMargin?: string }): React.RefObject<HTMLElement>
export const SCROLL_ANIMATION_STYLES: string  // CSS for .animate-on-scroll + .animate-on-scroll.animate-in
```

From components/marketing/navbar.tsx:
```typescript
export default function Navbar(): JSX.Element
// Sticky nav, reads from NAV constants, handles scroll + mobile
```

From lib/marketing-constants.ts (after Plan 01 updates):
```typescript
const NAV = {
  logo: "Somosite",
  links: [
    { label: "How It Works", href: "#how-it-works" },
    { label: "Our Work", href: "#portfolio" },
    { label: "Pricing", href: "#pricing" },
    { label: "FAQ", href: "#faq" },
  ],
  cta: { label: "Contact Us", href: "#contact" },
} as const

const HERO = {
  headline: "A professional website built from your real business data",
  subheadline: "We research your actual business — your services, reviews, and market — then build a custom website designed to convert visitors into customers. No templates. No stock content. Ready in days.",
  primaryCta: "See Our Work",
  secondaryCta: "View Pricing",
  primaryCtaHref: "#portfolio",
  secondaryCtaHref: "#pricing",
  trustSignal: "500+ Websites Delivered",
} as const

const TRUST_SIGNALS = [
  { icon: "CheckCircle", value: "50+", label: "Sites Delivered" },
  { icon: "Zap", value: "48hr", label: "Turnaround" },
  { icon: "Shield", value: "100%", label: "Satisfaction Guarantee" },
  { icon: "Code", value: "Modern", label: "Tech Stack" },
] as const

const PROBLEM = {
  headline: "Your Website Is Losing You Customers",
  body: "Every day, potential customers visit your site and leave within seconds. ...",
  emphasis: "You deserve a website that works as hard as you do.",
} as const
```

CSS custom properties from layout.tsx:
```
--mkt-bg: #0A0A0A, --mkt-bg-alt: #FAFAFA, --mkt-accent: #AF92FF
--mkt-text: #FFFFFF, --mkt-text-secondary: rgba(255,255,255,0.7)
--mkt-text-tertiary: rgba(255,255,255,0.5)
--mkt-border: rgba(255,255,255,0.08)
--mkt-surface: rgba(255,255,255,0.03)
--mkt-transition: 0.16s ease-out, --mkt-radius: 10px
--mkt-max-width: 1200px
Headings: font-family: var(--font-dm-serif)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero section with browser mockup and float animation</name>
  <files>components/marketing/hero.tsx</files>
  <action>
Create `components/marketing/hero.tsx` as a "use client" component.

**Layout structure (LOCKED from CONTEXT section 3.2):**
```
<section id="hero">
  <div container, two-column on desktop, single column on mobile>
    <div left column — text content>
      <h1> HERO.headline — large display font, 48-64px desktop, 32-40px mobile </h1>
      <p> HERO.subheadline — 18-20px, muted text </p>
      <div CTA row>
        <a primary> HERO.primaryCta — #AF92FF bg, white text, large (px-8 py-4), rounded </a>
        <a secondary> HERO.secondaryCta — text link style, underline or subtle border </a>
      </div>
      <p trust signal> HERO.trustSignal — small, muted, with a shield or check icon from lucide-react </p>
    </div>
    <div right column — browser mockup (hidden on mobile, visible lg+)>
      BrowserMockup component
    </div>
  </div>
</section>
```

On mobile (below lg:1024px): single column — text first, then mockup below (or hide mockup entirely on very small screens below sm:640px).

**Browser Mockup (LOCKED from CONTEXT — CSS-drawn frame, Claude's discretion on implementation):**

Build inline as a sub-component within the file (no separate file needed):

```
<div mockup-wrapper — relative, with float animation>
  <div desktop-frame — rounded-xl, border, shadow, overflow-hidden>
    <div title-bar — h-8, flex, 3 dots (red/yellow/green circles), dark bg>
      <div dot red /> <div dot yellow /> <div dot green />
      <span url bar text — centered, small, muted />
    </div>
    <div screen — aspect-video, bg-gradient placeholder>
      <!-- Placeholder gradient simulating a website screenshot -->
      <!-- Use a gradient from dark to slightly lighter with accent hints -->
      <!-- Real screenshots come in a future milestone -->
    </div>
  </div>
  <div mobile-frame — absolute, positioned bottom-right, offset, smaller scale>
    <div mobile-shell — rounded-2xl, border, shadow>
      <div mobile-notch — centered, w-20, h-1, rounded-full, bg-dark />
      <div mobile-screen — aspect-[9/16] cropped, same gradient placeholder />
    </div>
  </div>
</div>
```

**Float animation (LOCKED — CSS keyframes only):**
- Add a CSS `@keyframes float` animation to the mockup wrapper:
  ```css
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-12px); }
  }
  ```
- Apply: `animation: float 6s ease-in-out infinite`
- Use an inline `<style>` tag within the component for the keyframe (or export it to be added to layout styles)
- The float should be gentle and slow — 6 second cycle, 12px movement

**Scroll animation:**
- Import `useScrollAnimation` from `@/hooks/use-scroll-animation`
- Apply ref to the hero section wrapper so it fades in on load (threshold: 0.1 since it's at the top)
- Actually, the hero is already visible on load. Apply scroll animation only to the text content div (not the whole section). Or simply skip the scroll animation for the hero since it's above the fold — the mockup float animation is sufficient. Use scroll animation on the text block for a staggered reveal feel on initial load.

**Responsive:**
- Desktop (lg+): Two-column grid (text left 55%, mockup right 45%)
- Tablet (sm-lg): Single column, text then mockup
- Mobile (<sm): Single column, text only (hide mockup to avoid clutter)

**Section padding:** `pt-32 sm:pt-40 lg:pt-48 pb-16 sm:pb-24` (extra top padding for fixed navbar clearance ~80px)

**Imports:**
- `import { HERO } from '@/lib/marketing-constants'`
- `import { useScrollAnimation } from '@/hooks/use-scroll-animation'`
- `import { Shield } from 'lucide-react'` (for trust signal icon next to "500+ Websites Delivered")

Keep under 130 lines. No shadcn/Radix. No image files needed (placeholder gradients).
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit components/marketing/hero.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
- Hero renders headline, subheadline, two CTAs, trust signal text, and browser mockup
- Browser mockup shows desktop frame with title bar dots and gradient placeholder + floating mobile frame
- Float animation runs via CSS keyframes (6s cycle)
- Responsive: two-column on desktop, single column on mobile
- All text from HERO constants
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Trust Bar, Problem section, and wire everything into page.tsx</name>
  <files>components/marketing/trust-bar.tsx, components/marketing/problem.tsx, app/(marketing)/page.tsx, app/(marketing)/layout.tsx</files>
  <action>
**A) Create `components/marketing/trust-bar.tsx`:**

"use client" component for the trust signal strip below the hero.

Structure (LOCKED from CONTEXT section 3.3):
```
<section id="trust">
  <div container, grid cols-2 on mobile, cols-4 on sm+>
    {TRUST_SIGNALS.map(signal => (
      <div — flex col, items-center, text-center>
        <LucideIcon /> — Render the correct Lucide icon based on signal.icon string
        <p value — text-2xl, font-bold />
        <p label — text-sm, muted />
      </div>
    ))}
  </div>
</section>
```

**Lucide icon rendering:**
- Import the 4 specific icons used: `CheckCircle`, `Zap`, `Shield`, `Code` from lucide-react
- Create a simple icon map: `const ICON_MAP = { CheckCircle, Zap, Shield, Code } as const`
- Look up `signal.icon` in the map to render: `const Icon = ICON_MAP[signal.icon as keyof typeof ICON_MAP]`
- Icon styling: `w-6 h-6 text-[var(--mkt-accent)] mb-3`

**Section styling (LOCKED):**
- Background: `bg-[var(--mkt-bg-alt)]` (#FAFAFA light section)
- Text: `text-[var(--mkt-text-dark)]` (#1A1A1A)
- Padding: `py-12 sm:py-16`
- Grid: `grid-cols-2 sm:grid-cols-4 gap-8 sm:gap-12`
- Apply `useScrollAnimation` ref to the section

**B) Create `components/marketing/problem.tsx`:**

"use client" component for the problem/empathy section.

Structure (LOCKED from CONTEXT section 3.4):
```
<section id="problem">
  <div container, max-w-3xl, mx-auto, text-center>
    <h2> PROBLEM.headline — large heading </h2>
    <p> PROBLEM.body — lg text, relaxed leading, muted color </p>
    <p> PROBLEM.emphasis — xl text, accent color, medium weight </p>
  </div>
</section>
```

**Section styling (LOCKED):**
- Dark background (default --mkt-bg)
- Generous whitespace: `py-20 sm:py-28 lg:py-32`
- Text centered, max-w-3xl for readability
- No images, no icons — pure typography
- Apply `useScrollAnimation` ref to the content wrapper

**C) Update `app/(marketing)/page.tsx`:**

Replace the current monolithic page with component imports. The page becomes a thin shell that imports and composes components.

```tsx
import Navbar from '@/components/marketing/navbar'
import Hero from '@/components/marketing/hero'
import TrustBar from '@/components/marketing/trust-bar'
import Problem from '@/components/marketing/problem'
// Keep inline sections for how-it-works, portfolio, benefits, pricing, faq, final-cta, contact, footer
// (those will be extracted to components in Phases 18-20)
```

Page structure:
```
<>
  <Navbar />
  <Hero />
  <TrustBar />
  <Problem />
  {/* How It Works — keep existing inline section */}
  {/* Portfolio — keep existing inline section */}
  {/* Benefits — keep existing inline section */}
  {/* Pricing — keep existing inline section */}
  {/* FAQ — keep existing inline section */}
  {/* Final CTA — keep existing inline section */}
  {/* Contact — keep existing inline section */}
  {/* Footer — keep existing inline section */}
</>
```

IMPORTANT: Add `"use client"` directive to page.tsx since it now imports client components. Or alternatively, keep page.tsx as a server component and let the client components handle their own "use client" boundary — Next.js App Router allows importing client components from server components. Prefer the latter (keep page.tsx as server component).

Remove the old inline Hero, Trust, and Problem sections from page.tsx. Keep all other sections (how-it-works through footer) inline as they were — those get extracted in later phases.

Add `scroll-margin-top: 80px` to each remaining section via a Tailwind class `scroll-mt-20` so that nav anchor links account for the fixed navbar height.

**D) Update `app/(marketing)/layout.tsx`:**

Inject the `SCROLL_ANIMATION_STYLES` CSS into the existing `<style>` tag.

- Import `SCROLL_ANIMATION_STYLES` from `@/hooks/use-scroll-animation`
- Append it to the existing CSS string inside `dangerouslySetInnerHTML`
- This makes the `.animate-on-scroll` and `.animate-on-scroll.animate-in` classes available globally within the marketing layout

Also add to the existing CSS:
```css
.marketing section {
  scroll-margin-top: 80px;
}
```
This ensures all section anchor links account for the fixed navbar.
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit app/\(marketing\)/page.tsx app/\(marketing\)/layout.tsx components/marketing/trust-bar.tsx components/marketing/problem.tsx 2>&1 | head -30</automated>
  </verify>
  <done>
- Trust bar renders 4 items with correct Lucide icons, values, and labels in horizontal row (2x2 on mobile)
- Problem section renders centered empathetic copy with headline, body, and emphasis
- page.tsx imports Navbar, Hero, TrustBar, Problem as components; remaining sections stay inline
- layout.tsx includes scroll animation CSS styles
- All sections have scroll-margin-top for fixed nav offset
- Scroll animations fire on TrustBar and Problem sections when scrolled into view
- No TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all modified files
2. `npm run build` completes without errors (or `next build`)
3. Visit `/` — Navbar sticks to top, transitions on scroll
4. Hero shows headline, CTAs, browser mockup with float animation
5. Trust bar shows 4 items with icons below hero
6. Problem section centered with emphasis text in accent color
7. Scroll down — sections animate in with fade/slide
8. Mobile (375px): hamburger nav, single-column hero, 2x2 trust grid
9. Nav links scroll smoothly to correct sections with proper offset
</verification>

<success_criteria>
- All 4 components (Navbar, Hero, TrustBar, Problem) render correctly on the landing page
- Scroll-triggered animations work on TrustBar and Problem sections
- Hero browser mockup floats with CSS keyframe animation
- Navigation is sticky with transparent-to-solid transition
- Mobile responsive at 375px, 640px, 1024px breakpoints
- All copy reads from marketing-constants.ts
- No hardcoded text in components, no animation libraries, no shadcn/Radix
</success_criteria>

<output>
After completion, create `.planning/phases/17-hero-trust-problem/17-02-SUMMARY.md`
</output>
