---
phase: 19-pricing-faq-final-cta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/marketing-constants.ts
  - components/marketing/pricing.tsx
  - components/marketing/faq.tsx
  - components/marketing/final-cta.tsx
  - app/(marketing)/page.tsx
autonomous: true
requirements: [PAGE-08, PAGE-09, PAGE-10]

must_haves:
  truths:
    - "Visitor sees 3 pricing cards: Standard $499, Pro $1,299 (Most Popular badge), Premium Custom"
    - "Pro card is visually elevated with accent border/glow and Most Popular badge"
    - "Pricing cards show anchoring statement above and guarantee/no-hidden-fees below"
    - "Get Started buttons on Standard/Pro scroll to #contact, Contact Us on Premium scrolls to #contact"
    - "On mobile, Pro card appears first (reordered from desktop layout)"
    - "FAQ accordion shows 7 questions, only one open at a time, with smooth expand/collapse animation"
    - "Clicking an open FAQ question collapses it; clicking another opens it and collapses the previous"
    - "Final CTA section has distinct dark background treatment, urgency headline, scarcity line, and two CTA buttons"
    - "All three sections animate in on scroll via useScrollAnimation"
  artifacts:
    - path: "components/marketing/pricing.tsx"
      provides: "3-tier pricing cards with Pro elevation, payment badges, guarantee badge"
      min_lines: 60
    - path: "components/marketing/faq.tsx"
      provides: "Hand-coded accordion with single-open state, CSS height transition, chevron rotation"
      min_lines: 40
    - path: "components/marketing/final-cta.tsx"
      provides: "Urgency-driven CTA section with scarcity line and dual buttons"
      min_lines: 30
    - path: "lib/marketing-constants.ts"
      provides: "Updated PRICING/FAQ/FINAL_CTA data matching PRD-locked copy"
      contains: "anchoring"
    - path: "app/(marketing)/page.tsx"
      provides: "Imports Pricing, FAQ, FinalCTA components replacing inline sections"
  key_links:
    - from: "components/marketing/pricing.tsx"
      to: "lib/marketing-constants.ts"
      via: "import { PRICING }"
      pattern: "import.*PRICING.*from.*marketing-constants"
    - from: "components/marketing/faq.tsx"
      to: "lib/marketing-constants.ts"
      via: "import { FAQ }"
      pattern: "import.*FAQ.*from.*marketing-constants"
    - from: "components/marketing/final-cta.tsx"
      to: "lib/marketing-constants.ts"
      via: "import { FINAL_CTA }"
      pattern: "import.*FINAL_CTA.*from.*marketing-constants"
    - from: "app/(marketing)/page.tsx"
      to: "components/marketing/pricing.tsx"
      via: "import Pricing"
      pattern: "import Pricing from.*marketing/pricing"
---

<objective>
Build the 3 conversion sections of the landing page: Pricing (3-tier cards with Pro elevation), FAQ (hand-coded accordion), and Final CTA (urgency-driven). Update marketing constants to match PRD copy, create the 3 components, and wire them into page.tsx replacing inline placeholders.

Purpose: These are the conversion-critical sections that turn visitors into leads. Pricing provides anchoring + social proof, FAQ handles objections, Final CTA creates urgency.
Output: 3 new components, updated constants, clean page.tsx with all Phase 19 sections as imports.
</objective>

<execution_context>
@/Users/sohail/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sohail/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-pricing-faq-final-cta/19-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/marketing-constants.ts:
```typescript
export type PricingTier = (typeof PRICING.tiers)[number]
export type FAQItem = (typeof FAQ.items)[number]

// PRICING.tiers[n] shape:
// { name, price, period, description, features: string[], cta, ctaHref, highlighted, badge? }

// FAQ.items[n] shape:
// { question: string, answer: string }

// FINAL_CTA shape:
// { headline, subheadline, primaryCta, secondaryCta, primaryCtaHref, secondaryCtaHref }
```

From hooks/use-scroll-animation.ts:
```typescript
export function useScrollAnimation(
  options?: { threshold?: number; rootMargin?: string }
): React.RefObject<HTMLElement | null>
// Usage: const sectionRef = useScrollAnimation()
// Attach to section element: ref={sectionRef as React.RefObject<HTMLElement>}
```

Established component patterns (from benefits.tsx, portfolio.tsx):
- "use client" directive at top
- Import constants from @/lib/marketing-constants
- Import useScrollAnimation from @/hooks/use-scroll-animation
- Lucide icon map pattern: `const ICON_MAP = { ... } as const`
- Section wrapper: `<section id="xxx" ref={sectionRef as React.RefObject<HTMLElement>} className="py-16 sm:py-24 scroll-mt-20">`
- Dark sections: default (no bg class)
- Light/alt sections: `bg-[var(--mkt-bg-alt)] text-[var(--mkt-text-dark)]`
- Max width container: `mx-auto max-w-[var(--mkt-max-width)] px-4 sm:px-6 lg:px-8`
- Section title: `text-[28px] sm:text-[40px] lg:text-[48px] leading-tight text-center`
- Design tokens: --mkt-accent (#AF92FF), --mkt-border, --mkt-surface, --mkt-surface-hover, --mkt-text-secondary, --mkt-text-tertiary, --mkt-radius
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update marketing constants and create Pricing, FAQ, FinalCTA components</name>
  <files>
    lib/marketing-constants.ts
    components/marketing/pricing.tsx
    components/marketing/faq.tsx
    components/marketing/final-cta.tsx
  </files>
  <action>
**1. Update lib/marketing-constants.ts -- PRICING constant:**

Replace PRICING with PRD-locked copy. Add `anchoring` field at top level:
```
anchoring: "Traditional web design agencies charge $3,000-$10,000 and take 8-12 weeks. We eliminated the overhead -- not the quality."
```

Update tier features to match PRD exactly:

Standard -- $499:
- Custom website built from your business data
- Mobile-responsive design
- SEO-ready structure
- Free subdomain (yourname.somosite.com)
- Custom domain setup support
- 30-day satisfaction guarantee

Pro -- $1,299 (badge: "Most Popular"):
- Everything in Standard
- Online booking system integration
- Priority delivery
- Onboarding call with our team
- Advanced customization support

Premium -- Custom (period: "quote"):
- Enterprise-level websites
- Custom integrations
- Dedicated support

Keep `highlighted: true` on Pro, `badge: "Most Popular"` on Pro.
All CTAs: Standard/Pro = "Get Started" href="#contact", Premium = "Contact Us" href="#contact".

Add below-cards fields:
```
guarantee: "30-Day Satisfaction Guarantee"
noHiddenFees: "No hidden fees. One-time payment. You own everything."
```

**2. Update lib/marketing-constants.ts -- FAQ constant:**

Replace FAQ items with the 7 PRD-locked questions:

1. Q: "How is this different from Wix or Squarespace?" -- A: "Those are DIY tools where you build from templates. We research your business and build a custom site for you. The result looks like a $5,000 agency site, not a template."
2. Q: "What if I don't like the design?" -- A: "Request changes before going live. We offer a 30-day satisfaction guarantee."
3. Q: "Do I own my website?" -- A: "Yes. 100% ownership. Your code, your domain, your content. No lock-in."
4. Q: "Are there recurring costs?" -- A: "It's a one-time purchase. The only recurring cost is domain registration (~$10-15/year) if you want a custom domain."
5. Q: "How do you know about my business?" -- A: "We research using publicly available information -- your services, location, reviews, and photos. Your website reflects your actual business, not generic content."
6. Q: "How long does it take?" -- A: "Your preview is ready within a few days. Go live within 48 hours after approval."
7. Q: "Can I upgrade later?" -- A: "Yes. Standard clients can upgrade to Pro anytime. We apply the difference."

**3. Update lib/marketing-constants.ts -- FINAL_CTA constant:**

Replace with PRD-locked copy:
```
headline: "Ready to see what we can build for your business?"
subheadline: "Join hundreds of businesses that went from invisible to professional in days."
primaryCta: "Get Your Website"
secondaryCta: "Or contact us to discuss your project"
primaryCtaHref: "#contact"
secondaryCtaHref: "#contact"
scarcity: "We take on a limited number of clients each month."
```

**4. Create components/marketing/pricing.tsx:**

"use client" component. Import PRICING from constants, useScrollAnimation from hook, Check and Shield icons from lucide-react.

Structure:
- Section with `id="pricing"`, light/alt background (`bg-[var(--mkt-bg-alt)] text-[var(--mkt-text-dark)]`), scroll animation ref
- Anchoring statement paragraph above the section title (italic or subdued style, centered, max-w-3xl)
- Section title + subtitle
- 3-card grid: `grid-cols-1 sm:grid-cols-3 gap-8` on desktop
- **Mobile reorder:** On mobile (below sm), Pro card appears first. Use CSS `order` classes: Pro card gets `order-first sm:order-none` or render with explicit mobile ordering via flex + order utilities
- **Pro card elevation:** Slightly larger scale or more padding, `border-2 border-[var(--mkt-accent)]` (vs `border border-gray-200` for others), subtle shadow (`shadow-lg shadow-[var(--mkt-accent)]/10`), "Most Popular" badge as absolute-positioned pill at top
- **Standard/Premium cards:** Subtle border `border border-gray-200` (light bg context), rounded-xl, p-8
- Feature list: Each feature with a Check icon (lucide-react) in accent color + feature text
- CTA buttons: Pro gets filled accent button, Standard/Premium get outline buttons. All `href="#contact"`
- **Below cards:** Guarantee badge (shield icon + "30-Day Satisfaction Guarantee") centered, then "No hidden fees..." text
- **Payment badges:** Simple text-based row showing "Visa | Mastercard | UPI" or small SVG payment icons (Claude's discretion -- text is fine for MVP)
- Card hover: `hover:scale-[1.02] transition-transform duration-200` + shadow increase

**5. Create components/marketing/faq.tsx:**

"use client" component with useState for `openIndex: number | null`.

Structure:
- Section with `id="faq"`, dark background (default, no bg-alt class), scroll animation ref
- Section title "Frequently Asked Questions"
- Accordion container: `max-w-3xl mx-auto mt-12` with items separated by `border-b border-[var(--mkt-border)]`
- Each FAQ item is a div with:
  - Button trigger: full-width flex row with question text (left) + ChevronDown icon (right)
  - ChevronDown rotates 180deg when open: `transform transition-transform duration-300` with conditional `rotate-180`
  - Answer container: CSS max-height transition for smooth expand/collapse:
    `overflow-hidden transition-all duration-300 ease-out` with conditional `max-h-96 opacity-100` vs `max-h-0 opacity-0`
  - Answer text: `pb-4 pt-2 text-[var(--mkt-text-secondary)] leading-relaxed`
  - onClick handler: `setOpenIndex(openIndex === i ? null : i)` (toggles current, closes others)
- No shadcn, no Radix -- fully hand-coded accordion per PRD decision

**6. Create components/marketing/final-cta.tsx:**

"use client" component (needs useScrollAnimation).

Structure:
- Section with `id="final-cta"`, dark background with distinct treatment -- use a subtle radial gradient or a darker bg variant to differentiate from other dark sections. Suggestion: `bg-gradient-to-b from-[#0F0F12] to-[#0A0A0A]` or similar subtle gradient (Claude's discretion)
- Scroll animation ref
- Larger text treatment: headline uses `text-[32px] sm:text-[48px] lg:text-[56px]` (bigger than other sections)
- Subheadline below
- **Scarcity line:** Smaller italic text below subheadline, slightly muted: `text-sm italic text-[var(--mkt-text-secondary)]`
- Two CTA buttons centered: Primary "Get Your Website" (filled accent, large `px-8 py-4`), Secondary "Or contact us to discuss your project" (outline/text link style)
- Generous vertical padding: `py-24 sm:py-32`
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - marketing-constants.ts has PRD-exact copy for PRICING (with anchoring, guarantee, noHiddenFees, exact tier features), FAQ (7 PRD questions), FINAL_CTA (with scarcity line)
    - pricing.tsx renders 3-tier cards with Pro elevation (accent border, badge, shadow), feature checkmarks, mobile Pro-first order, guarantee badge + payment badges below
    - faq.tsx renders hand-coded accordion with useState, single-open behavior, CSS max-height transition, chevron rotation
    - final-cta.tsx renders urgency section with larger text, scarcity line, gradient background, dual CTAs
    - All 3 components use useScrollAnimation and read from marketing-constants
    - TypeScript compiles with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Pricing, FAQ, FinalCTA into page.tsx</name>
  <files>
    app/(marketing)/page.tsx
  </files>
  <action>
Update app/(marketing)/page.tsx:

1. Add imports at top:
```typescript
import Pricing from "@/components/marketing/pricing"
import FAQ from "@/components/marketing/faq"
import FinalCTA from "@/components/marketing/final-cta"
```

2. Replace the 3 inline sections (Pricing ~lines 28-88, FAQ ~lines 90-108, Final CTA ~lines 110-134) with:
```tsx
<Pricing />
<FAQ />
<FinalCTA />
```

3. Remove unused constant imports from the import block. After this change, PRICING, FAQ, and FINAL_CTA should no longer be imported in page.tsx (they are imported by the components directly). Keep CONTACT and FOOTER imports since those inline sections remain for Phase 20.

4. Clean import statement:
```typescript
import { CONTACT, FOOTER } from "@/lib/marketing-constants"
```

Final page.tsx section order should be:
Navbar > Hero > TrustBar > Problem > HowItWorks > Portfolio > Benefits > Pricing > FAQ > FinalCTA > Contact (inline) > Footer (inline)
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit --pretty 2>&1 | head -30 && node -e "const fs=require('fs'); const p=fs.readFileSync('app/(marketing)/page.tsx','utf8'); const has=['Pricing','FAQ','FinalCTA'].every(c=>p.includes('import '+c)); const noInline=!p.includes('tier.name'); console.log('Components imported:', has, 'Inline removed:', noInline)"</automated>
  </verify>
  <done>
    - page.tsx imports Pricing, FAQ, FinalCTA from components/marketing/
    - All 3 inline sections replaced with component tags
    - PRICING, FAQ, FINAL_CTA no longer imported in page.tsx (only CONTACT, FOOTER remain)
    - Section order is correct: 7 component imports + 3 new component imports + 2 inline sections
    - TypeScript compiles with no errors
    - No unused imports
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All 3 new component files exist in components/marketing/
3. page.tsx has no inline Pricing/FAQ/FinalCTA sections
4. marketing-constants.ts PRICING has anchoring field, PRD-exact tier features, guarantee and noHiddenFees fields
5. marketing-constants.ts FAQ has exactly 7 PRD-locked questions
6. marketing-constants.ts FINAL_CTA has scarcity field and PRD-locked copy
7. `grep -c "useScrollAnimation" components/marketing/pricing.tsx components/marketing/faq.tsx components/marketing/final-cta.tsx` shows 1 per file
</verification>

<success_criteria>
- Three new marketing components (pricing.tsx, faq.tsx, final-cta.tsx) render correctly
- Pricing: 3-tier cards, Pro elevated with badge, anchoring above, guarantee below, mobile Pro-first
- FAQ: 7-question accordion, single-open, smooth CSS animation, hand-coded (no UI library)
- Final CTA: urgency copy, scarcity line, distinct background, dual CTAs to #contact
- All copy matches PRD exactly (no creative liberties on locked content)
- page.tsx is clean with component imports, no inline sections for Phase 19 content
</success_criteria>

<output>
After completion, create `.planning/phases/19-pricing-faq-final-cta/19-01-SUMMARY.md`
</output>
