---
phase: 11-auth-infrastructure-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260325000001_create_client_requests.sql
  - supabase/migrations/20260325000002_add_claims_auth_user_id.sql
  - supabase/migrations/20260325000003_add_projects_cal_embed_slug.sql
  - types/database.ts
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, AUTH-05]

must_haves:
  truths:
    - "The client_requests table exists with correct columns, CHECK constraints for type and status ENUMs, RLS enabled, and policies scoped to auth_user_id"
    - "The claims table has a nullable auth_user_id UUID column with FK to auth.users"
    - "The projects table has a nullable cal_embed_slug TEXT column"
    - "TypeScript types in database.ts match the new schema exactly"
  artifacts:
    - path: "supabase/migrations/20260325000001_create_client_requests.sql"
      provides: "client_requests table with RLS"
      contains: "CREATE TABLE public.client_requests"
    - path: "supabase/migrations/20260325000002_add_claims_auth_user_id.sql"
      provides: "claims.auth_user_id column"
      contains: "ADD COLUMN auth_user_id"
    - path: "supabase/migrations/20260325000003_add_projects_cal_embed_slug.sql"
      provides: "projects.cal_embed_slug column"
      contains: "ADD COLUMN cal_embed_slug"
    - path: "types/database.ts"
      provides: "TypeScript types for all schema changes"
      contains: "client_requests"
  key_links:
    - from: "types/database.ts"
      to: "supabase/migrations/20260325000001_create_client_requests.sql"
      via: "manual type synchronization"
      pattern: "type.*logo_upload.*text_change.*domain_setup.*agent_call.*booking_setup"
    - from: "supabase/migrations/20260325000001_create_client_requests.sql"
      to: "auth.users"
      via: "foreign key on auth_user_id"
      pattern: "REFERENCES auth\\.users"
---

<objective>
Create the three SQL migrations for Phase 11 schema changes and update TypeScript types to match.

Purpose: Every subsequent v3.0 phase depends on these tables and columns existing. This is the data layer foundation.
Output: 3 migration files in supabase/migrations/, updated types/database.ts
</objective>

<execution_context>
@/Users/sohail/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sohail/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-auth-infrastructure-schema/11-CONTEXT.md
@.planning/phases/11-auth-infrastructure-schema/11-RESEARCH.md
@types/database.ts

<interfaces>
<!-- Existing database.ts pattern: Each table has Row, Insert, Update, Relationships -->
<!-- claims.Row currently has: id, project_id, status, plan, amount_paise, currency, razorpay_*, client_*, domain_*, expires_at, paid_at, webhook_event_id, created_at, updated_at -->
<!-- projects.Row currently has: id, batch_id, business_data, generated_code, design_language, status, version, thumbnail_url, generation_phase, error_type, error_details, prompt_version_id, quality_score, slug, claim_expires_at, screenshot_url, created_at, updated_at -->

From types/database.ts (existing pattern for type unions):
```typescript
type: 'logo_upload' | 'text_change' | 'domain_setup' | 'agent_call' | 'booking_setup'
status: 'pending' | 'in_progress' | 'completed'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration files</name>
  <files>
    supabase/migrations/20260325000001_create_client_requests.sql
    supabase/migrations/20260325000002_add_claims_auth_user_id.sql
    supabase/migrations/20260325000003_add_projects_cal_embed_slug.sql
  </files>
  <action>
Create three migration files exactly as specified in the research document:

**Migration 1 (20260325000001_create_client_requests.sql):**
- CREATE TABLE public.client_requests with columns: id (UUID PK, gen_random_uuid()), claim_id (UUID NOT NULL FK to claims), project_id (UUID NOT NULL FK to projects), auth_user_id (UUID NOT NULL FK to auth.users), type (TEXT NOT NULL with CHECK constraint for the 5 locked types: logo_upload, text_change, domain_setup, agent_call, booking_setup), status (TEXT NOT NULL DEFAULT 'pending' with CHECK for 3 locked statuses: pending, in_progress, completed), content (JSONB NOT NULL DEFAULT '{}'), admin_notes (TEXT nullable), created_at (TIMESTAMPTZ NOT NULL DEFAULT now()), updated_at (TIMESTAMPTZ NOT NULL DEFAULT now())
- Create indexes: idx_client_requests_claim_id, idx_client_requests_project_id, idx_client_requests_auth_user_id, idx_client_requests_status, idx_client_requests_created_at (DESC)
- Enable RLS: ALTER TABLE public.client_requests ENABLE ROW LEVEL SECURITY
- Create SELECT policy "clients_select_own" TO authenticated USING ((SELECT auth.uid()) = auth_user_id)
- Create INSERT policy "clients_insert_own" TO authenticated WITH CHECK ((SELECT auth.uid()) = auth_user_id)
- No UPDATE/DELETE policies (admin uses service role which bypasses RLS)
- Create OR REPLACE set_updated_at() trigger function (idempotent in case it already exists)
- Create trigger client_requests_updated_at BEFORE UPDATE

**Migration 2 (20260325000002_add_claims_auth_user_id.sql):**
- ALTER TABLE public.claims ADD COLUMN auth_user_id UUID REFERENCES auth.users(id) (nullable, no CASCADE -- claim records must survive auth user deletion)
- CREATE INDEX idx_claims_auth_user_id ON public.claims(auth_user_id)

**Migration 3 (20260325000003_add_projects_cal_embed_slug.sql):**
- ALTER TABLE public.projects ADD COLUMN cal_embed_slug TEXT (nullable, no constraints)

Use exact SQL from the research code examples section. Do NOT use CREATE TYPE for enums -- use CHECK constraints per the research pattern.
  </action>
  <verify>
    <automated>ls -la /Users/sohail/Documents/Antigravity/WebGen/webgen/supabase/migrations/20260325*.sql && grep -l "client_requests" /Users/sohail/Documents/Antigravity/WebGen/webgen/supabase/migrations/20260325000001_create_client_requests.sql && grep -l "auth_user_id" /Users/sohail/Documents/Antigravity/WebGen/webgen/supabase/migrations/20260325000002_add_claims_auth_user_id.sql && grep -l "cal_embed_slug" /Users/sohail/Documents/Antigravity/WebGen/webgen/supabase/migrations/20260325000003_add_projects_cal_embed_slug.sql</automated>
  </verify>
  <done>Three migration files exist with correct DDL. client_requests has all 10 columns, 5 indexes, RLS enabled, 2 policies (SELECT + INSERT), and updated_at trigger. claims has auth_user_id column with FK and index. projects has cal_embed_slug column.</done>
</task>

<task type="auto">
  <name>Task 2: Update TypeScript types in database.ts</name>
  <files>types/database.ts</files>
  <action>
Update types/database.ts to reflect the three schema changes. Follow the EXACT existing pattern (Row/Insert/Update/Relationships structure):

**Add client_requests table type** inside public.Tables (add alphabetically between claim_events and claims):
- Row: all columns required (id: string, claim_id: string, project_id: string, auth_user_id: string, type union of 5 types, status union of 3 statuses, content: Json, admin_notes: string | null, created_at: string, updated_at: string)
- Insert: id/status/content/admin_notes/created_at/updated_at optional, claim_id/project_id/auth_user_id/type required
- Update: all optional
- Relationships: two entries -- client_requests_claim_id_fkey to claims, client_requests_project_id_fkey to projects

**Update claims table type:**
- Add to Row: auth_user_id: string | null
- Add to Insert: auth_user_id?: string | null
- Add to Update: auth_user_id?: string | null

**Update projects table type:**
- Add to Row: cal_embed_slug: string | null
- Add to Insert: cal_embed_slug?: string | null
- Add to Update: cal_embed_slug?: string | null

Type union values MUST match the CHECK constraints in the SQL exactly:
- Request types: 'logo_upload' | 'text_change' | 'domain_setup' | 'agent_call' | 'booking_setup'
- Request statuses: 'pending' | 'in_progress' | 'completed'
  </action>
  <verify>
    <automated>cd /Users/sohail/Documents/Antigravity/WebGen/webgen && npx tsc --noEmit types/database.ts 2>&1 | head -20</automated>
  </verify>
  <done>types/database.ts compiles without errors. client_requests type exists with correct Row/Insert/Update/Relationships. claims.Row includes auth_user_id: string | null. projects.Row includes cal_embed_slug: string | null.</done>
</task>

</tasks>

<verification>
1. All three migration files exist in supabase/migrations/ with correct timestamps
2. types/database.ts compiles without TypeScript errors
3. client_requests type has all 5 request types and 3 statuses matching SQL CHECK constraints
4. claims type includes auth_user_id, projects type includes cal_embed_slug
5. RLS policies use `(SELECT auth.uid()) = auth_user_id` pattern with `TO authenticated`
</verification>

<success_criteria>
- 3 new SQL migration files in supabase/migrations/
- types/database.ts updated with client_requests table, claims.auth_user_id, projects.cal_embed_slug
- TypeScript compiles without errors
- Migration SQL uses CHECK constraints (not CREATE TYPE) for enum values
- RLS enabled on client_requests with SELECT and INSERT policies scoped to auth_user_id
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-infrastructure-schema/11-01-SUMMARY.md`
</output>
